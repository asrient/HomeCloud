{
  "name": "pine",
  "version": "0.0.0",
  "private": true,
  "author": "ASRIENT",
  "workspaces": [
    "ui"
  ],
  "scripts": {
    "build": "pnpm --filter @pine/* build",
    "build-cli": "go build -o ./bin/homecloud ./cli",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "mobile-init": "go get -d golang.org/x/mobile/cmd/gomobile && gomobile init",
    "bind": "gomobile bind -o ./ios/Libx.xcframework -target=ios ./libx"
  },
  "devDependencies": {
    "@capacitor/cli": "^5.0.5",
    "mobile": "^0.0.1",
    "prettier": "latest"
  },
  "engines": {
    "node": ">=14.0.0"
  },
  "packageManager": "pnpm@7.26.3"
}